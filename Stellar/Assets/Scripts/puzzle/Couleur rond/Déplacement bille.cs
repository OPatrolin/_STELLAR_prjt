using UnityEngine;

public class Déplacementbille : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

// if col tag ploacement et bille, un des 2 emplacement dispo (collider sus et les 2 colliderr des placement) échanger la bille de place sur le placement